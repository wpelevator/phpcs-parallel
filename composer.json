{
    "name": "wpelevator/pharallel",
    "description": "Run one command per matched path, optionally in parallel.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "symfony/console": "^6.4 || ^7.0",
        "symfony/process": "^6.4 || ^7.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5",
        "phpstan/phpstan": "^2.2"
    },
    "autoload": {
        "psr-4": {
            "WPElevator\\PHPCSParallel\\": "src/"
        }
    },
    "bin": [
        "bin/pharallel"
    ],
    "scripts": {
        "lint": "phpcs",
        "format": "phpcbf",
        "analyse": "phpstan analyse",
        "test": "phpunit",
        "test:coverage": "phpunit --coverage-text --coverage-clover=tests/coverage/clover.xml --coverage-html=tests/coverage/html"
    }
}
